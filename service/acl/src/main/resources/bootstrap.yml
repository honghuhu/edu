spring:
  application:
    name: acl
  cloud:
    nacos:
      # discovery:
        # server-addr: ${config_server_uri:${CONFIG_SERVER_URI:http://localhost:8848}}
        # namespace: drs
      config:
        # namespace: drs
        server-addr: ${config_server_uri:${CONFIG_SERVER_URI:http://localhost:8848}}
        file-extension: yml